--- src/notify.c
+++ src/notify.c
@@ -440,7 +440,7 @@
     }
 
     // إضافة الإشعار للسجل
-    add_to_history(n->id, app_name, icon, summary, body, desktop_entry);
+    add_to_history(n->id, app_name, icon, summary, body, desktop_entry, value);
 
     notify_ui_reposition(active_notifications, notify_use_layer_shell);
 
